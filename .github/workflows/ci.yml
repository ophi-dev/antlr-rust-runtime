name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write
  issues: write
  # Codecov's tokenless upload authenticates via an OIDC token minted by the
  # workflow, so the job must be allowed to request one.
  id-token: write

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Clippy and Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Install stable Rust
        run: |
          rustup toolchain install stable --profile minimal --component clippy,llvm-tools --no-self-update
          rustup default stable

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-llvm-cov

      - name: Validate direct-codegen fixtures
        run: node tools/grammar-frontend/validate-interp-fixtures.mjs

      - name: Validate port evidence
        run: node tools/grammar-frontend/validate-port-evidence.mjs

      - name: Validate workspace release metadata
        run: tools/release/check-workspace-version.sh

      - name: Run Clippy
        run: cargo clippy --locked --workspace --all-targets --all-features -- -D warnings

      # `cargo llvm-cov` runs the same suite as `cargo test` but under LLVM
      # source-based instrumentation, so it replaces the plain test step and
      # emits an lcov report in one pass. Line/region coverage only — branch
      # coverage needs the nightly-only `-Z coverage-options=branch`.
      - name: Run unit tests with coverage
        run: cargo llvm-cov --locked --all-features --workspace --lcov --output-path lcov.info

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v7
        with:
          files: lcov.info
          flags: unittests
          use_oidc: true
          # Surface upload problems instead of silently passing, but do not
          # fail the build for external Codecov availability blips.
          fail_ci_if_error: false
          disable_search: true

      - name: Validate publishable package archives
        run: tools/release/preflight-package-archives.sh

      - uses: ophi-dev/mehen@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          paths: |
            crates/antlr-rust-runtime/src
            crates/antlr-rust-codegen/src
            crates/antlr-rust-g4-parser/src
            crates/antlr-rust-rs-parser/src
            tests/antlr-rust-runtime-testsuite/src

  msrv:
    name: Rust 1.95
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Install Rust 1.95
        run: rustup toolchain install 1.95 --profile minimal --no-self-update

      - name: Check workspace at MSRV
        run: cargo +1.95 check --locked --workspace --all-targets --all-features
